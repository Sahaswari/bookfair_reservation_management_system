services:
  # Backend Services
  
  # API Gateway Service
  api-gateway:
    build:
      context: ./backend/api-gateway
      dockerfile: Dockerfile
    container_name: bookfair-api-gateway
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8080
      - AUTH_SERVICE_URI=http://auth-service:8081
      - STALL_SERVICE_URI=http://stall-service:8085
      - RESERVATION_SERVICE_URI=http://reservation-service:8084
      - GENRE_SERVICE_URI=http://genre-service:8082
      - NOTIFICATION_SERVICE_URI=http://notification-service:8083
      - GATEWAY_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
      - APP_JWT_SECRET=${APP_JWT_SECRET:-ChangeMePlease_ToASuperStrongJwtSecret_2025}
    networks:
      - bookfair-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  # Auth Service
  auth-service:
    build:
      context: ./backend/auth-service
      dockerfile: Dockerfile
    container_name: bookfair-auth-service
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8081
      - SPRING_DATASOURCE_URL=jdbc:postgresql://auth-db:5432/auth_db
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - APP_KAFKA_ENABLED=true
      - APP_KAFKA_USER_EVENTS_TOPIC=user-events
      - APP_JWT_SECRET=${APP_JWT_SECRET:-ChangeMePlease_ToASuperStrongJwtSecret_2025}
    networks:
      - bookfair-network
    depends_on:
      auth-db:
        condition: service_healthy
    restart: unless-stopped

  # Genre Service
  genre-service:
    build:
      context: ./backend/genre-service
      dockerfile: Dockerfile
    container_name: bookfair-genre-service
    ports:
      - "8082:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8082
      - SPRING_DATASOURCE_URL=jdbc:postgresql://genre-db:5432/genre_db
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
    networks:
      - bookfair-network
    depends_on:
      genre-db:
        condition: service_healthy
    restart: unless-stopped

  # Notification Service
  notification-service:
    build:
      context: ./backend/notification-service
      dockerfile: Dockerfile
    container_name: bookfair-notification-service
    ports:
      - "8083:8083"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8083
      - SPRING_DATASOURCE_URL=jdbc:postgresql://notification-db:5432/notification_db
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
    networks:
      - bookfair-network
    depends_on:
      notification-db:
        condition: service_healthy
    restart: unless-stopped

  # Reservation Service
  reservation-service:
    build:
      context: ./backend/reservation-service
      dockerfile: Dockerfile
    container_name: bookfair-reservation-service
    ports:
      - "8086:8086"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8084
      - SPRING_DATASOURCE_URL=jdbc:postgresql://reservation-db:5432/reservation_db
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
    networks:
      - bookfair-network
    depends_on:
      reservation-db:
        condition: service_healthy
    restart: unless-stopped

  # Stall Service
  stall-service:
    build:
      context: ./backend/stall-service
      dockerfile: Dockerfile
    container_name: bookfair-stall-service
    ports:
      - "8085:8085"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8085
      - SPRING_DATASOURCE_URL=jdbc:postgresql://stall-db:5432/stall_db
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - APP_KAFKA_ENABLED=true
      - APP_KAFKA_USER_EVENTS_TOPIC=user-events
      - APP_KAFKA_CONSUMER-GROUP-ID=stall-service-user-sync
      - APP_KAFKA_LISTENER-CONCURRENCY=2
      - APP_KAFKA_RETRY_MAX-ATTEMPTS=5
      - APP_KAFKA_RETRY_INITIAL-INTERVAL-MS=2000
      - APP_KAFKA_RETRY_MAX-INTERVAL-MS=60000
      - APP_KAFKA_RETRY_MULTIPLIER=2.0
    networks:
      - bookfair-network
    depends_on:
      stall-db:
        condition: service_healthy
    restart: unless-stopped

  # PostgreSQL Database for Stall Service
  stall-db:
    image: postgres:16-alpine
    container_name: bookfair-stall-db
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=stall_db
    volumes:
      - stall-db-data:/var/lib/postgresql/data
    networks:
      - bookfair-network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    restart: unless-stopped

  auth-db:
    image: postgres:16-alpine
    container_name: bookfair-auth-db
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=auth_db
    volumes:
      - auth-db-data:/var/lib/postgresql/data
    networks:
      - bookfair-network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    restart: unless-stopped

  reservation-db:
    image: postgres:16-alpine
    container_name: bookfair-reservation-db
    ports:
      - "5435:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=reservation_db
    volumes:
      - reservation-db-data:/var/lib/postgresql/data
    networks:
      - bookfair-network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    restart: unless-stopped

  genre-db:
    image: postgres:16-alpine
    container_name: bookfair-genre-db
    ports:
      - "5436:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=genre_db
    volumes:
      - genre-db-data:/var/lib/postgresql/data
    networks:
      - bookfair-network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    restart: unless-stopped

  notification-db:
    image: postgres:16-alpine
    container_name: bookfair-notification-db
    ports:
      - "5437:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=notification_db
    volumes:
      - notification-db-data:/var/lib/postgresql/data
    networks:
      - bookfair-network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    restart: unless-stopped

  zookeeper:
    image: bitnami/zookeeper:3.9
    container_name: bookfair-zookeeper
    ports:
      - "2181:2181"
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    networks:
      - bookfair-network
    restart: unless-stopped

  kafka:
    image: bitnami/kafka:3.8
    container_name: bookfair-kafka
    ports:
      - "9094:9094"
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,PLAINTEXT_HOST://:9094
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:9094
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE=true
      - ALLOW_PLAINTEXT_LISTENER=yes
    depends_on:
      - zookeeper
    networks:
      - bookfair-network
    restart: unless-stopped

  # Frontend Services

  # Employee Portal
  employee-portal:
    build:
      context: ./frontend/employee-portal
      dockerfile: Dockerfile
    container_name: bookfair-employee-portal
    ports:
      - "3000:80"
    environment:
      # Add API endpoint configuration
      - REACT_APP_API_URL=http://localhost:8080
      # Or for Vue: VUE_APP_API_URL=http://localhost:8080
      # Or for Angular: use environment files
    networks:
      - bookfair-network
    depends_on:
      - api-gateway
    restart: unless-stopped

  # User Portal
  user-portal:
    build:
      context: ./frontend/user-portal
      dockerfile: Dockerfile
    container_name: bookfair-user-portal
    ports:
      - "3001:80"
    environment:
      # Add API endpoint configuration
      - REACT_APP_API_URL=http://localhost:8080
      # Or for Vue: VUE_APP_API_URL=http://localhost:8080
      # Or for Angular: use environment files
    networks:
      - bookfair-network
    depends_on:
      - api-gateway
    restart: unless-stopped

  # Optional: Database Services (uncomment if needed)
  
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: bookfair-postgres
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  #     - POSTGRES_DB=bookfair_db
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #     - ./init-scripts:/docker-entrypoint-initdb.d
  #   networks:
  #     - bookfair-network
  #   restart: unless-stopped

  # redis:
  #   image: redis:7-alpine
  #   container_name: bookfair-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis-data:/data
  #   networks:
  #     - bookfair-network
  #   restart: unless-stopped

  # mysql:
  #   image: mysql:8.0
  #   container_name: bookfair-mysql
  #   ports:
  #     - "3306:3306"
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=root
  #     - MYSQL_DATABASE=bookfair_db
  #     - MYSQL_USER=bookfair
  #     - MYSQL_PASSWORD=bookfair
  #   volumes:
  #     - mysql-data:/var/lib/mysql
  #   networks:
  #     - bookfair-network
  #   restart: unless-stopped

networks:
  bookfair-network:
    driver: bridge

volumes:
  stall-db-data:
  auth-db-data:
  reservation-db-data:
  genre-db-data:
  notification-db-data:
#   postgres-data:
#   redis-data:
#   mysql-data:
